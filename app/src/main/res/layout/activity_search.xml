<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.theshaeffers.musicplayer.SearchActivity">

    <EditText
        android:id="@+id/search_entry_edittext"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:inputType="textCapWords"
        android:maxLength="50"
        android:hint="Search for a Song, Artist, or Playlist"/>

    <TextView
        android:layout_above="@+id/search_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="This will take the users input and search via data pulled from the Cursor Resolver. The results will be diplayed in a ListView."
        android:textSize="24sp"
        />

    <Button
        android:id="@+id/search_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp"
        android:text="@string/search_string" />

</RelativeLayout>
